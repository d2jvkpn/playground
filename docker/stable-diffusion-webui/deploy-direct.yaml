version: '3'

services:
  sd-webui:
    image: sd-webui:${TAG}
    container_name: sd-webui_${APP_ENV}
    restart: always
    user: 1000:1000
    networks: ["net"]
    # ports: ["127.0.0.1:7860:7860"]
    ports: ["7860:7860"]
    environment: ["TZ=Asia/Shanghai"]
    deploy:
      resources:
        # limits: { cpus: "4", memory: 15g }
        reservations:
          # cpus: "1"
          # memory: 4g
          devices:
          - driver: nvidia
            # device_ids: ['0', '3']
            # count: 1
            count: all
            capabilities: [gpu]
    volumes:
    - ./outputs:/home/hello/sd-webui/outputs
    - ./data/models:/home/hello/sd-webui/models
    # - ./data/extentions/sd-webui-controlnet:/home/hello/sd-webui/extensions/sd-webui-controlnet/annotator/downloads
    ## hed, lineart_anime, mlsd, pidinet
    # - ./data/cache:/home/hello/.cache
    # - ./data/interrogate:/home/hello/sd-webui/interrogate
    command:
    - /entrypoint.sh
    - --xformers
    - --listen
    - --api
    - --port=7860
    # - --ckpt=models/Stable-diffusion/realisticVisionV13_v13.safetensors

networks:
  net:
    name: sd-webui_${APP_ENV}
    driver: bridge
    external: false
