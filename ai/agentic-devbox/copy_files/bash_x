if [ -d "$HOME/apps/bin" ]; then
    export PATH="$HOME/apps/bin:$PATH"
fi

for d in $(ls -d $HOME/apps/*/ 2>/dev/null); do
    if [ -d "${d}bin" ]; then
        export PATH="${d}bin:$PATH"
    fi
done
