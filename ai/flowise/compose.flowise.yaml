services:
  flowise:
    container_name: flowise
    image: flowiseai/flowise:latest
    restart: always
    network_mode: bridge
    ports: ["3000:3000"]
    volumes:
    - ./data/faiss:/root/faiss
    - ./data/flowise:/root/.flowise
