services:
  port-forward:
    image: nginx:1
    container_name: port-forward
    #restart: unless-stopped
    restart: always
    #network_mode: host
    network_mode: bridge
    ports: ["8136:8136"]
    volumes:
    - ./configs/nginx.conf:/etc/nginx/nginx.conf:ro
    - ./logs:/var/log/nginx
