SELECT DISTINCT ON (user_id) *
FROM records
ORDER BY user_id, timestamp DESC;

SELECT DISTINCT ON (user_id) *
FROM records
ORDER BY user_id, timestamp DESC;

SELECT DISTINCT ON (user_id) *
FROM records
ORDER BY user_id, timestamp DESC;

SELECT DISTINCT ON (sn) sn, date
FROM t_inverter_data
ORDER BY sn, date DESC;
