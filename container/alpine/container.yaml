version: "3"

services:
  alpine:
    image: localhost/alpine:3
    container_name: alpine
    volumes:
    - ./data/alpine:/app:ro
    environment: ["TZ=Asia/Shanghai"]
    working_dir: /app
    command: [tail, -f, /etc/hosts]
