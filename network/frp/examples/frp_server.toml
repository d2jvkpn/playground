bindAddr = "0.0.0.0"
bindPort = 3050
# vhostHTTPPort = 80
# vhostHTTPSPort = 443
# vhostHTTPTimeout = 60
userConnTimeout = 10

log.to = "logs/frp_server.log"
auth.method = "token"
auth.token = "connection_token"

transport.tls.force = true
transport.tls.trustedCaFile = "configs/frp_ca.crt"
transport.tls.certFile = "configs/frp_server.crt"
transport.tls.keyFile = "configs/frp_server.key"

# webServer.addr = "127.0.0.1"
# webServer.port = 3051
# webServer.user = "admin"
# webServer.password = "admin"
