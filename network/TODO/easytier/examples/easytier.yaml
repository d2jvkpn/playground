network:
  name: mesh-001
  cidr: 10.1.0.0/24

node:
  name: node-a
  mode: peer

security:
  enable_encryption: true
  # 32 bytes key
  shared_key: $secret

discovery:
  enable: true
  servers:
  - "udp://1.2.3.4:11010"
  - "udp://5.6.7.8:11010"

transport:
  udp:
    enable: true

log:
  level: info
